{
  "name": "pharmasync-api",
  "private": true,
  "type": "module",
  "description": "Pharmasync backend: a Neon Function serving the Android app (Postgres + Object Storage, Firebase Auth tokens).",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "jose": "^5.9.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
